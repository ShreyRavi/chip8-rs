[package]
name = "chip8-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
chip8-core = { path = "../chip8-core" }
wasm-bindgen = "0.2"
console_error_panic_hook = "0.1"
